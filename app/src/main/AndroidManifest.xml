<?xml version="1.0" encoding="utf-8"?>
<!--
  SPDX-FileCopyrightText: 2023 The LineageOS Project
  SPDX-License-Identifier: Apache-2.0

  LibreDialer Compose — migrated from crDroid/LineageOS Dialer
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    coreApp="true"
    package="org.librelab.dialer">

    <!-- Phone / Telecom -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />
    <!-- Required for appearing in the system default-dialer picker -->
    <uses-feature android:name="android.software.app_dialer" android:required="false" />
    <uses-feature android:name="android.software.dialer" android:required="false" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.WRITE_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
    <uses-permission android:name="android.permission.MANAGE_ROUTES" />
    <uses-permission android:name="android.permission.ANSWER_PHONE_CALLS" />

    <!-- System UI -->
    <uses-permission android:name="android.permission.READ_SYNC_SETTINGS" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Account -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.MANAGE_ACCOUNTS" />
    <uses-permission android:name="android.permission.AUTHENTICATE_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />

    <!-- Location (for assisted dialing) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Camera -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- NFC -->
    <uses-permission android:name="android.permission.NFC" />

    <!-- Voicemail -->
    <uses-permission android:name="org.librelab.voicemail.permission.ADD_VOICEMAIL" />
    <uses-permission android:name="org.librelab.voicemail.permission.READ_VOICEMAIL" />
    <uses-permission android:name="org.librelab.voicemail.permission.WRITE_VOICEMAIL" />

    <!-- Foreground service types -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />

    <application
        android:name=".LibreDialerApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.LibreDialer"
        android:enabled="true"
        tools:targetApi="36">

        <!-- ===================== MAIN DIALER ===================== -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.LibreDialer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Dial from contacts / other apps -->
            <intent-filter>
                <action android:name="android.intent.action.DIAL" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="tel" />
            </intent-filter>
            <!-- Dial shortcut widget -->
            <intent-filter>
                <action android:name="android.intent.action.DIAL" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme=" Dialpad" />
            </intent-filter>
            <!-- ACTION_CALL_BUTTON from Intent -->
            <intent-filter>
                <action android:name="android.intent.action.CALL_BUTTON" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Call Log detail -->
        <activity
            android:name=".ui.calllog.CallLogDetailActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <!-- Floating Dialpad Popup Window -->
        <activity
            android:name=".ui.dialpad.DialpadActivity"
            android:theme="@style/Theme.LibreDialer"
            android:exported="false"
            android:windowSoftInputMode="stateAlwaysHidden|adjustResize" />

        <!-- Call Details -->
        <activity
            android:name=".ui.calldetails.CallDetailsActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <!-- Settings -->
        <activity
            android:name=".ui.settings.DialerSettingsActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <!-- ===================== IN-CALL UI ===================== -->
        <!-- InCallService handles the actual call screen, but we also need -->
        <!-- an Activity entry point when InCallUI is brought to foreground -->
        <activity
            android:name=".ui.incall.InCallActivity"
            android:exported="false"
            android:launchMode="singleInstance"
            android:theme="@style/Theme.LibreDialer.InCall"
            android:excludeFromRecents="true"
            android:taskAffinity="" />

        <!-- ===================== VOICEMAIL ===================== -->
        <activity
            android:name=".ui.voicemail.VoicemailActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <!-- ===================== SYSTEM SERVICES ===================== -->

        <!-- Telecom InCallService — provides in-call UI to system Telecom.
             IN_CALL_SERVICE_UI=true: Telecom uses our service as the in-call screen UI.
             IN_CALL_SERVICE_RINGING=false: We handle incoming call UI ourselves via our Compose UI. -->
        <service
            android:name=".service.InCallServiceImpl"
            android:permission="android.permission.BIND_INCALL_SERVICE"
            android:exported="true"
            android:foregroundServiceType="phoneCall">
            <intent-filter>
                <action android:name="android.telecom.InCallService" />
            </intent-filter>
            <meta-data
                android:name="android.telecom.IN_CALL_SERVICE_UI"
                android:value="true" />
            <meta-data
                android:name="android.telecom.IN_CALL_SERVICE_RINGING"
                android:value="false" />
            <meta-data
                android:name="android.telecom.INCLUDE_EXTERNAL_CALLS"
                android:value="true" />
        </service>

        <!-- Call Screening Service for antispam -->
        <service
            android:name=".service.CallScreeningServiceImpl"
            android:permission="android.permission.BIND_SCREENING_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>
        </service>

        <!-- Phone Account Registration -->
        <service
            android:name=".service.PhoneAccountRegistrationService"
            android:exported="false" />

        <!-- Voicemail source -->
        <service
            android:name=".service.VoicemailServiceImpl"
            android:permission="android.permission.BIND_VOICEMAIL"
            android:exported="true">
            <intent-filter>
                <action android:name="android.telecom.VoicemailService" />
            </intent-filter>
        </service>

        <!-- ===================== BROADCAST RECEIVERS ===================== -->

        <!-- Missed call notification receiver -->
        <receiver
            android:name=".service.MissedCallNotificationReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="org.librelab.dialer.ACTION_SHOW_MISSED_CALLS" />
            </intent-filter>
        </receiver>

        <!-- Outgoing call receiver -->
        <receiver
            android:name=".service.OutgoingCallReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.NEW_OUTGOING_CALL" />
            </intent-filter>
        </receiver>

        <!-- BlockReportSpam dialog receiver -->
        <receiver
            android:name=".service.BlockReportSpamDialogReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="org.librelab.dialer.ACTION_SHOW_BLOCK_REPORT_DIALOG" />
            </intent-filter>
        </receiver>

        <!-- Contact shortcut -->
        <activity-alias
            android:name="org.librelab.dialer.callcontact"
            android:targetActivity=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.CALL_PRIMARY" />
            </intent-filter>
            <meta-data
                android:name="android.intent.extra.CALL_PRIMARY_EMAIL"
                android:value="android.telecom.CallDiscoveryService" />
        </activity-alias>

        <!-- Contacts app integration -->
        <activity-alias
            android:name="org.librelab.dialer.shortcut"
            android:targetActivity=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.CREATE_SHORTCUT" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity-alias>

        <!-- QuickContact window -->
        <activity
            android:name=".ui.contactinfo.ContactInfoActivity"
            android:exported="false"
            android:theme="@style/Theme.LibreDialer.Dialog" />

        <!-- Dialtacts speed dial -->
        <activity-alias
            android:name="org.librelab.dialer.speeddial"
            android:targetActivity=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.DIAL" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:mimeType="vnd.android.cursor.dir/phone" />
            </intent-filter>
        </activity-alias>

    </application>
</manifest>
